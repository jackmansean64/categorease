{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Flask Server",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/app/server_flask.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "env": {
                "FLASK_ENV": "development",
                "FLASK_DEBUG": "1"
            }
        }
    ]
}